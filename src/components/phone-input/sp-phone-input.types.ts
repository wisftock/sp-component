export interface SpPhoneCountry {
  code: string;   // ISO 3166-1 alpha-2, e.g. "US"
  dial: string;   // e.g. "+1"
  name: string;   // e.g. "United States"
  flag: string;   // emoji flag, e.g. "🇺🇸"
}
